# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env files. *.local above already covers .env.local / .env.*.local (Vite's
# own convention); .env itself needs its own line. .env.example is
# intentionally NOT ignored — it's checked in as documentation.
.env

# Python backend
backend/.venv
backend/__pycache__
backend/**/__pycache__
*.pyc

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
